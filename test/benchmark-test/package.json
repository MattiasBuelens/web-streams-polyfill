{
  "name": "benchmark-test",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "benchmark": "npm run benchmark:queue-size && npm run benchmark:read-buffered",
    "benchmark:queue-size": "node queue-size.js",
    "benchmark:read-buffered": "node read-buffered.js"
  },
  "devDependencies": {
    "@stardazed/streams": "^3.1.0",
    "benchmark": "^2.1.4",
    "web-streams-polyfill": "*"
  }
}
