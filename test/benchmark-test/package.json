{
  "name": "benchmark-test",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "benchmark": "npm run benchmark:queue-size && npm run benchmark:read-buffered",
    "benchmark:queue-size": "node --allow-natives-syntax queue-size.js",
    "benchmark:read-buffered": "node --allow-natives-syntax read-buffered.js"
  },
  "devDependencies": {
    "bench-node": "^0.15.0",
    "web-streams-polyfill": "*",
    "web-streams-polyfill-baseline": "npm:web-streams-polyfill@^4.3.0"
  }
}
